bug记录：
1、20250512:
	最近发现，用了21的虚拟线程后，并且支持了带返回值的@ZAsync后，在几个平台压测
	armbian/ubuntu/debian，jdk21都会迅速出现nio线程cpu100%，升级jdk25/24后缓解，但
	还是会出现。问了deepseek，改了一些代码，主要是及时关闭客户端已经断掉的连接，
	但是还是会出现。
	本次记录如下：
		在ubuntu16虚拟机1核4G，-Xmx2g启动
		http://192.168.88.129:5555/asyncL
		测试87并行度 测试1800多万次时停止压测，
		又复现了。
		arthas监控 thread 出现在下面这行：
		final int select = this.selector.select();
		
	可能很早之前就有这个问题，只是这几天刚支持了带返回值的@ZAsync，测试了才发现？
	
	chromebook 4+16 2955U openjdk25 测试如下：
	修改为 select(500)后：
	两个接口同时测试：
		/i32 5千万次和 /asyncL 1500万次后停止，cpu占用马上降到0了，
		没出现nio线程占100%，select={}的日志也是正常的500毫秒输出一次0
		
		?? 是升级jdk25的原因，还是用了带超时的select的原因？
	
	
		再次测试如下：同时开启两个进程，唯一区别是端口号不同、一个打印select()返回值一个不打印
		nohup java -Xmx200m -jar zf1-1.0.0-jdk21_ZResponseWRITE.jar --server.port=400 --server.print.nio.select=true >> 400_select_TRUE.log &
		nohup java -Xmx200m -jar zf1-1.0.0-jdk21_ZResponseWRITE.jar --server.port=500 --server.print.nio.select=false >> 500_select_FALSE.log &
		
		如下测试：
		16.234 是 4+16 2955u 的chromebool openjdk25
		88.138 是 2G   2核debina虚拟机，  openjdk24
		四个java测试进程同时停止，结果如下：
		16.234 两个进程 都出现select() 这行占cpu100%了
			400 这个select() 一直返回195，停了好几分钟还没完并且cpu一直100%；
			500 那个在arthas查看和400一样，也是nio线程在select()这行占100%；
			
		看来怀疑的是由于开启了打印日志，是不是cpu耗在了打印日志这里，看来还是怀疑的不对。
		
		2026-05-15T15:10:24.589015900	当前k = 170500000	qps = 5619.535310902192 URL = http://192.168.16.234:500/asyncL
		2026-05-15T15:10:24.007575500	当前k = 127140000	qps = 4191.872771748769 URL = http://192.168.16.234:400/asyncL
		2026-05-15T15:10:24.556103		当前k = 57475000		qps = 1895.4047243939726 URL = http://192.168.88.138:500/asyncL
		2026-05-15T15:10:18.140212300	当前k = 57760000		qps = 1905.8753623505936 URL = http://192.168.88.138:400/asyncL
	